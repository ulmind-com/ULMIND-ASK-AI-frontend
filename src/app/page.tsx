'use client';

export default function Home() {
  return (
    <main className="min-h-screen bg-transparent pointer-events-none">
      {/* The ChatWidget is rendered globally in layout.tsx */}
    </main>
  );
}
